describe("infragen", () => {
  it.todo(
    "should generate a basic React app with TypeScript, Babel, Webpack, Jest, React, and a hello world page"
  );
  it.todo(
    "should have a jest and cypress test ensuring that it works as expected"
  );
  it.todo("should build, test, and deploy to our K8S cluster");
});
