describe("@infragen/generator-add-ts", () => {
  it.todo("should run `yarn add --dev typescript`");

  it.todo(
    "should run `yarn add --dev ts-node @types/node` if it's in the context of a Node App"
  );

  it.todo("should add start script to the package.json file");
  // {
  //   scripts: {
  //     start: "yarn ts-node ."
  //   }
  // };

  it.todo("should create a basic tsconfig.json file");
  // {
  //   "compilerOptions": {
  //     "preserveSymlinks": true
  //   }
  // }
});
